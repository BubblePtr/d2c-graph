你正在分析一张移动端界面截图。

你的任务是根据提供的界面截图，生成一份自然语言视觉锚点描述，用于辅助后续根据 Figma 节点数据生成更合理的响应式代码。

目标不是复述界面细节，而是提炼视觉布局关系、组件语义、设计模式和视觉层级。

结构要求：
1. 输出内容必须严格包含四个部分，且顺序固定为：页面概览 → 组件语义 → 设计模式 → 视觉层级。
2. 每个部分都要使用明确标题，标题必须与上述名称一致。
3. 描述使用陈述句，避免疑问句和祈使句。
4. 每个部分至少包含 2 条有效信息。

内容约束：
1. 页面概览描述整体布局类型和主要功能区域，用 2 到 3 句话说明页面如何被划分。
2. 组件语义识别具体组件类型，例如 Button、Card、Input、Nav，并说明它们承担的功能角色。
3. 设计模式说明布局系统，例如栅格、Flex、Grid，以及间距规则和对齐方式。
4. 视觉层级指出主要信息、次要信息、辅助信息的区分方式，重点描述大小、颜色倾向、位置关系等相对差异。

质量标准：
1. 避免重复 Figma 数据中已有的精确数值，不要写按钮宽度、字号或具体间距数值。
2. 聚焦设计意图而非表象，优先描述主操作按钮、信息卡片、导航区域等角色，不要只描述颜色或装饰。
3. 使用相对描述而非绝对描述，例如间距一致、对齐统一、主信息更突出、次要信息靠后。
4. 不要猜测业务逻辑、交互逻辑或组件实现细节。
5. 如果不确定，用保守描述。

返回 JSON，对象格式：
{{
  "visual_anchors": "### 页面概览\n1. ...\n2. ...\n\n### 组件语义\n1. ...\n2. ...\n\n### 设计模式\n1. ...\n2. ...\n\n### 视觉层级\n1. ...\n2. ..."
}}
